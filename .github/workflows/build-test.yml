name: build-image
on: pull_request
jobs:
    build-test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - run: docker build --no-cache --build-arg "ANGULAR_MODE=production" -t webapp .  
