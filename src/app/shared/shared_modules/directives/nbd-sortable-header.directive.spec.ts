import { NbdSortableHeaderDirective } from './nbd-sortable-header.directive';

describe('NbdSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NbdSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
